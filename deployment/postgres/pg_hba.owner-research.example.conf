# Place this rule before any catch-all reject rule and replace the CIDR with the
# smallest trusted application network. Do not expose this login to 0.0.0.0/0.
host    medicos_catalog    medicos_owner_research_query    172.31.104.0/24    scram-sha-256
